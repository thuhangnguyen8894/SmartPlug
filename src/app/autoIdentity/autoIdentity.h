#ifndef __autoIdentity_H_
#define __autoIdentity_H_

#include <iostream>
#include <string.h>
#include <sstream>

extern unsigned int numTableTimerInt;

char* idTableTimer();
char* idTableSmartDevice();

#endif