mysql> CREATE TRIGGER followElectric AFTER UPDATE
    -> ON SmartDevice
    -> FOR EACH ROW
    -> BEGIN
    -> DECLARE idFollow CHAR(20);
    -> DECLARE nameFollow CHAR(20);
    -> DECLARE stateFollow CHAR(20);
    -> DECLARE dateFollow CHAR(20);
    -> IF NEW.stateElectric <> OLD.stateElectric
    -> THEN
    -> SET idFollow=OLD.idSmartDevice;
    -> SET nameFollow=OLD.nameSmartDevice;
    -> SET stateFollow=NEW.stateElectric;
    -> SET dateFollow=NOW();
    -> INSERT INTO Follow_stateElectric VALUES(idFollow,nameFollow,stateFollow,dateFollow);
    -> END IF;
    -> END//
